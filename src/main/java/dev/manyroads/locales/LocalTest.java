package dev.manyroads.locales;

import java.util.Locale;

/**
 * Locale in conjunction with DateFormat/DateTimeFormatter, NumberFormat and MessageFormat
 * in a format specific to a country
 */
public class LocalTest {

    Locale duck = Locale.of("Duckiaans","Duckland","dorps");
    Locale de = Locale.of("de" ,"DE", "WIN");

}
